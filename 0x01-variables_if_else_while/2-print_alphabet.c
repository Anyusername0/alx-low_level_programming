#include <stdio.h>

/**
 * main - entry point
 *
 * Description: print alphabets in lowercase
 *
 * Return: 0 (success)
*/

int main(void)

	char al = 'a';
{
	while (al <= 'z')
        putchar(al);
        l++
}
	putchar('\n');
	return (0);
