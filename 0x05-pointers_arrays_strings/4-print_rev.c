#include "main.h"

/**
 * print_rev - imprime en reversa
 * @s: string
 * return: 0
 */

void print_rev(char *s)
{
	int b = 0;

	while (s(b))
		b++;

	while (b--)
		_putchar(s(b));

	_putchar('\n');
}
